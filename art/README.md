# art/

Approved concept art, style guide (`style/STYLE.md`), UI kit, textures. Only small approved files (<5 MB) live here; renders/iterations go to R2 or `art/renders/` (git-ignored). Log every image-gen prompt in `art/PROMPTS.md` and every visual-iteration score in `art/DIFF.md`.
